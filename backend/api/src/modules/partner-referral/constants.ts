/** Settings key backing the configurable partner-referral rate. */
export const PARTNER_REFERRAL_RATE_SETTING_KEY = 'rewards.partner_referral_rate_bps';

/** Default rate when the setting is unset: 500 bps = 5% (P3.1). */
export const DEFAULT_PARTNER_REFERRAL_RATE_BPS = 500;
