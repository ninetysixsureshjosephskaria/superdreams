{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile --prod=false --config.confirmModulesPurge=false && pnpm --filter @superdreams/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @superdreams/api release && pnpm --filter @superdreams/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
