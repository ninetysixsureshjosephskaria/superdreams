# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Test & coverage
coverage/

# Turborepo
.turbo/

# Caches
.cache/
.eslintcache

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Environment — commit templates only; never real secrets.
.env
.env.*
!.env.example
!.env.development
!.env.production.example
# Frontend production URLs are public (Vite bakes VITE_* into the client bundle),
# so the SPAs' .env.production is committed for deterministic production builds.
!apps/member/.env.production
!apps/bcc/.env.production

# Editor
.vscode/settings.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Temporary
*.tmp
*~

# Database backups (local dumps only)
infrastructure/backups/*.sql.gz
