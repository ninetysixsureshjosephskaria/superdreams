import { env } from './env';

/** Human-readable application name (from the environment). */
export const APP_NAME = env.VITE_APP_NAME;

/** Short label used in compact UI (e.g. the collapsed sidebar). */
export const APP_SHORT_NAME = 'Portal';

/** One-line application description. */
export const APP_DESCRIPTION = 'Member portal for the Super Dreams platform.';
