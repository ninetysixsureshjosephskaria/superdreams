# packages/auth

Client-side authentication utilities shared across applications: token
handling, session helpers, and auth-related hooks/guards.

- **Responsibility:** Client-side authentication helpers only.
- **Note:** Server-side authentication logic lives in the backend identity and
  authentication modules, not here.

> Placeholder. Implemented in a later phase. See `docs/dna/07-security.md`.
