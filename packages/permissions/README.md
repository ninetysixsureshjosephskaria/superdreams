# packages/permissions

Role-Based Access Control (RBAC) permission definitions and helper utilities
shared across the platform, enabling consistent permission checks on both the
backend and frontend.

- **Responsibility:** Permission definitions and evaluation helpers only.
- **Note:** Hidden UI is never a substitute for backend authorization.

> Placeholder. Implemented in a later phase. See `docs/dna/07-security.md`.
