# packages/config

Shared configuration for the platform. Centralizes configuration so it is not
duplicated across applications and services.

- **Responsibility:** Shared configuration only.

> Placeholder. Implemented in a later phase. See `docs/dna/02-repository.md`.
