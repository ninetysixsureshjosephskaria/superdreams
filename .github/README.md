# .github/

GitHub configuration for the Super Dreams platform.

## Contents

- `workflows/` — GitHub Actions CI/CD pipelines.

The continuous integration pipeline (install → lint → typecheck → test → build)
and related automation are defined in the DevOps phase. This directory is
created during bootstrap so the structure is in place.

See `docs/dna/09-devops.md`.
